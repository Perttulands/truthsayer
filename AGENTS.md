# AGENTS.md

## Ground Rules

- Every user-facing change gets a `CHANGELOG.md` entry ([Keep a Changelog](https://keepachangelog.com/en/1.1.0/)). Include what changed, why, and the date. No entry = no merge.
- Prefer `trash` over `rm` for cleanup.
- Never use forced git rewrites as a retry pattern.
# test
